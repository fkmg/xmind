1、当服务启动时在对应服务节点下(唯一的对应于每个服务名称)创建一个子节
点（临时节点）
2、当需要获取某一个服务A,可以通过服务A去对应节点下获取所有的子节点，
以对其进行缓存，然后实现一个客户端的负载均衡

3、对服务A有引用的节点还可以对服务A添加监听， 这样有新的节点加入，或者退出都能
查询到

4、Zookeeper中临时节点生命周期是和SESSION绑定的，如果SESSION超时了，对应的节
点会被删除，被删除时，Zookeeper 会通知对该节点父节点进行监听的客户端, 这样对应的客户
端又可以刷新本地缓存了

5、当有新服务加入时，同样也会通知对应的客户端，刷新本地缓存